language: node_js
node_js:
  - 11
cache:
  directories:
    - node_modules
script:
  - npm run test --coverage
branches:
  only:
    - travis
after_script:
  - cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage